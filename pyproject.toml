[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "multiferroic_rg"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["model*", "scripts*"]
